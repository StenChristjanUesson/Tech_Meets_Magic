@model TechMeetsMagic.Models.TheGeneratedUserMadeOpenWorld.TheUserMadeOpenWorldCreateViewModels

<div>
    <form method="post" asp-action="Update" enctype="multipart/form-data">
        
        @* <input asp-for="CreatedAt" type="hidden" />
        <input asp-for="UpdatedAt" type="hidden" />
        <input asp-for="NPCLevel" value="1" type="hidden" />
        <input asp-for="NPCStatus" value="Alive" type="hidden" /> *@

        <div class="form-group-row">
            <label asp-for="WorldName" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="WorldName" class="form-control" />
            </div>
        </div>

        <div class="form-group-row">
            <label asp-for="WorldDescription" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="WorldDescription" class="form-control" />
            </div>
        </div>

        <div class="form-group-row">
            <label asp-for="WorldImageUrl" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="WorldImageUrl" class="form-control" />
            </div>
        </div>

        <div class="form-group-row">
            <label asp-for="WorldMapUrl" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="WorldMapUrl" class="form-control" />
            </div>
        </div>

        <div class="form-group-row">
            <label asp-for="Files" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-5">
                <div class="custom-file">
                    <input class="form-control custom-file-input"
                           asp-for="Files"
                           multiple type="file" />
                    <label class="custom-file-label">Choose Images from device</label>
                </div>
            </div>
        </div>
        <div>
            @foreach(var image in Model.Images)
            {
                <partial name="_Images" model="image" />
                <input asp-action="removeImage" asp-controller="TheGeneratedWorlds" asp-route-id="@image.ImageID" type="submit" class="btn btn-danger" value="Delete Image" />
            }
        </div>
        <input asp-action="Update" asp-controller="TheGeneratedWorlds" type="submit" class="btn btn-primary" value="Change World" />
        <a asp-action="index" class="btn btn-primary">Cancel</a>

    </form>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('.custom-file-input').on("change", function () {
                var fileLabel = $(this).next('.custom-file-input');
                var files = $(this)[0].files;
                if (files.length > 1) {
                    files.html(files.length + ' files selected');
                }
                else if (files.length == 1) {
                    files.html(files[0].name);
                }
            });
        });
    </script>
}